# This workflow is superseded by deploy-main.yml which runs on push to main.
# Kept as a no-op placeholder to avoid breaking any branch protection rules
# that reference this workflow name.
name: Docker Build/Publish Beta (disabled)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is handled by deploy-main.yml on push to main."
